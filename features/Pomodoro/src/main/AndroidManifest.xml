<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.terry.pomodoro">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_pomodoro">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>

        <dist:fusing dist:include="true" />
    </dist:module>

    <application android:theme="@style/Theme.AndroidStandardLevel">
        <activity
            android:name=".PomodoroMainActivity"
            android:theme="@style/Theme.AndroidStandardLevel.Pomodoro">

        </activity>
    </application>

</manifest>