<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.terry.audioplayer">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_audioplayer">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>

        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity
            android:name=".AudioMainActivity"
            android:theme="@style/Theme.AndroidStandardLevel.AudioPlayer.NoActionBar">

        </activity>
    </application>

</manifest>